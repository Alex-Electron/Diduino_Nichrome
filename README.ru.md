# Diduino Nichrome

[English](./README.md) · **Русский**

Прошивка и браузерный программатор для советского биполярного ППЗУ **КР556РТ4** (256×4 бита, однократно программируемое).

Это продолжение открытого проекта **Diduino**. Железо то же самое; новое здесь — переписанное приложение, которое работает прямо в браузере, и алгоритм прожига, сделанный так, чтобы не убить чип, когда что-то идёт чуть-чуть не так.

<img src="img/app.png" alt="Веб-приложение Diduino Nichrome" width="100%">

![license](https://img.shields.io/badge/license-BSD--2--Clause-blue)
![platform](https://img.shields.io/badge/board-Arduino%20Nano-00979D?logo=arduino&logoColor=white)
![chip](https://img.shields.io/badge/chip-KR556RT4%20256x4-success)
![ui](https://img.shields.io/badge/host-Web%20Serial-1aff80)
![fw](https://img.shields.io/badge/firmware-v0.6.0-orange)

**▶ Открыть прошивальщик:** <https://alex-electron.github.io/Diduino_Nichrome/diduino_nichrome.html> — работает в Chrome или Edge, ставить ничего не нужно. (GitHub Pages отдаёт по HTTPS, а это и нужно Web Serial.)

<p align="center">
  <a href="https://alex-electron.github.io/Diduino_Nichrome/diduino_nichrome.html"><img src="img/launch.png" alt="Открыть прошивальщик Diduino Nichrome" height="54"></a>
</p>

---

## Авторы — оригинальный проект

Плата, схема и повышающий преобразователь — **не мои**. Они из открытого программатора **Diduino**, и вся честь за железо принадлежит его авторам.

<img src="img/board.png" alt="Плата Diduino" width="420">

**Оригинальный проект — главный репозиторий: <https://github.com/Radionews/diduino>**

- **Андрей Науменко (naym1993)** — автор Diduino, схема и печатная плата.
  EasyEDA: <https://oshwlab.com/naym1993/prog_did>
- **walhi** — базовый программатор, из которого всё выросло.
  <https://github.com/walhi/arduino_eprom27_programmer>
- **Ветка ProgRT** на форуме ZX-PK — где всё началось.
  <https://zx-pk.ru/threads/15617-programmator-ppzu-155re3-556rt4-i-drugikh.html>

Этот репозиторий добавляет только новую прошивку и веб-приложение. Лицензия осталась прежней — **BSD-2-Clause**.

---

## Зачем я переписал софт

Штатный Diduino работает. Три вещи всё-таки заставили переделать хост и прошивку:

1. **Веб-страница, которую просто открываешь.** Не надо качать, ставить и обновлять Qt-приложение. Один самодостаточный HTML-файл, открывается прямо с диска в Chrome или Edge и общается с платой через Web Serial. Больше ничего настраивать не нужно.

2. **Более аккуратная и надёжная прошивка.** Оригинальный скетч — это один движок чтения/записи. Я хотел, чтобы сам прожиг был осторожным: проверял чип до того, как его трогать, отказывался от битой передачи, выставлял напряжение программирования сам, а не надеялся, что я не забуду, и всегда оставлял плату в безопасном состоянии. КР556РТ4 однократное — одна ошибка губит чип, поэтому прошивка должна ловить очевидные промахи, а не послушно их выполнять.

3. **Soak-импульсы.** Перемычка, которая едва открылась, может прочитаться как прожжённая, а потом «прорасти» обратно. После того как бит взялся, прошивка добивает в него несколько импульсов (даташит разрешает 40–100), чтобы перемычка пережглась полностью, а не на грани. Это вышло из копания в спецификации и есть главная новая идея на стороне прожига.

---

## Состав репозитория

```
Diduino_Nichrome/
├── README.md                # английский
├── README.ru.md             # этот файл (русский)
├── LICENSE
├── firmware/
│   ├── Diduino_Nichrome/
│   │   └── Diduino_Nichrome.ino   # скетч Arduino (имя папки должно совпадать с .ino)
│   └── build-hex.ps1              # компилирует оба MCU и встраивает прошивку в страницу ниже
└── docs/
    └── diduino_nichrome.html      # браузерный программатор + встроенная прошивка (GitHub Pages, HTTPS)
```

---

## Чип: КР556РТ4

Советское биполярное ППЗУ с нихромовыми перемычками, 256 слов по 4 бита, аналог 82S129.

- Перемычки **односторонние**: бит может перейти 0 → 1, но никогда обратно. Чистый чип читается как одни нули.
- Для программирования нужно повышенное напряжение (~12.5 В) на ноге данных, при Vcc = 5 В.
- Чип одноразовый — на него ровно одна попытка. Смысл этого проекта в том, чтобы эта попытка удалась.

---

## Быстрый старт

1. **Открыть приложение.** Открой [прошивальщик по ссылке](https://alex-electron.github.io/Diduino_Nichrome/diduino_nichrome.html) в Chrome или Edge (либо `docs/diduino_nichrome.html` локально). Нажми CONNECT и выбери COM-порт платы.
2. **Прошивка.** Если на плате не Diduino_Nichrome (или версия старая), операции блокируются, и приложение предлагает **[ ⟳ FLASH NANO ]** — прошивка заливается прямо из браузера через Web Serial (без Arduino IDE): читается сигнатура чипа и подбирается нужная сборка (ATmega328P или ATmega168). Можно и по-старому — залить `firmware/Diduino_Nichrome/Diduino_Nichrome.ino` из Arduino IDE самому. После этого в шапке появится версия прошивки, а текущее Vpp считается автоматически.
3. **Читать или жечь.**
   - `READ` считывает все 256 нибблов и показывает hex и CRC-32.
   - `LOAD` открывает `.bin`; тот же hex-вид и CRC для файла.
   - `BURN` программирует чип. Запускается только если чип совместим и контрольная сумма передачи совпала, после чего сверяет каждую ячейку на плате.

Интерфейс по умолчанию русский; кнопка справа вверху переключает на английский.

---

## Прожиг по шагам («Nichrome»)

Прожиг устроен так, что обычные способы загубить чип ловятся до того, как напряжение дойдёт до перемычки:

| Этап | Что делает |
|------|------------|
| **Передача + CRC-32** | Хост шлёт 256 байт; плата считает CRC-32, и хост обязан прислать свою ожидаемую CRC. Не совпало — прожиг не запускается. Битый или не тот файл не пройдёт. |
| **Проверка совместимости** | Сначала чип читается. Если где-то уже стоит 1 там, где образу нужен 0 (на фьюз-ППЗУ это не отменить), прожиг прерывается со списком конфликтов. Чистый чип — или дожиг того же образа на частично прошитый — проходит. |
| **Проверка напряжения** | Плата сама выставляет уровень программирования (p1, ~12.5 В) и измеряет его. Жечь перемычку на неверном напряжении только потому, что уровень забыли, она не станет. |
| **Побитовая сверка** | Каждый бит записывается (прожигается) и читается; импульсы прекращаются, как только бит взялся. |
| **Soak** | Несколько добивочных импульсов после взятия бита, чтобы перемычка пережглась до конца. |
| **Сверка в покое** | Напряжение буста снижается до минимума, весь чип перечитывается и сверяется с образом. |
| **Безопасное состояние** | На любом выходе — успех, отмена или ошибка — плата заканчивает без импульса программирования, буст на минимуме, буфер порта вычищен. |

---

## Serial-протокол (115200 8N1, команды завершаются Enter)

| Команда | Значение |
|---------|----------|
| `?` | помощь |
| `v` | прочитать напряжение программирования (Vpp) |
| `p<n>` | уровень буста 0–7 (p1 ≈ 12.5 В для РТ4) |
| `I<n>` | макс. импульсов на бит (по умолчанию 1000) |
| `L<n>` | длительность импульса, мкс (по умолчанию 40) |
| `D<n>` | заполнение %, задаёт паузу между импульсами (по умолчанию 10) |
| `S<n>` | soak-импульсы после взятия бита (по умолчанию 50, потолок 128) |
| `R` | прочитать весь чип, 256 байт |
| `B` | прожиг: 256 байт, затем CRC-гейт, проверки и сверка |
| `T<cV>` | калибровка Vpp по мультиметру, в EEPROM (сантивольты, напр. `T1250`; см. [Калибровку](#калибровка-напряжения)) |
| `j` | безопасное состояние |

Есть и диагностический набор для наладки платы (`a c m f g e k W`) — проверка адресной цепочки, мукса, линии CS и одиночного импульса.

---

## Параметры по умолчанию

| Параметр | Команда | По умолчанию | Примечание |
|----------|---------|--------------|------------|
| Уровень Vpp | `p` | p1 (~12.5 В) | прожиг выставляет сам |
| импульсов/бит | `I` | 1000 | потолок; останавливается, когда бит взялся |
| длит. импульса | `L` | 40 мкс | |
| заполнение | `D` | 10 % | пауза = длит. × 100 / заполнение |
| soak | `S` | 50 | окно даташита 40–100 |

---

## Калибровка напряжения

**Зачем это нужно.** Плата читает Vpp через делитель на A4. Прошивка **не** опирается на фиксированное напряжение питания — она меряет Vpp *ратиометрически*, относительно внутреннего источника опорного напряжения 1.1 В в самом чипе. Поэтому показание верно **при любом питании** (лабораторный БП, штатный 7805 на плате или просто 5 В от USB Nano) и остаётся верным даже при просадке шины под нагрузкой. Это убирает старую проблему «у меня на 5 В показывает правильно, а у тебя — нет».

Единственное, чего прошивка про себя не знает точно, — это *точное* значение того внутреннего ИОН: номинал 1.1 В, но разброс около **±10 %** от чипа к чипу. Поэтому из коробки показание уже не зависит от питания, но по абсолютной величине точно лишь до ~±10 %. **Разовая калибровка** задаёт его точно, и результат пишется в **EEPROM** Nano — сделал один раз, и он переживает перезагрузки и любую дальнейшую смену источника питания.

**Когда заморачиваться.** Если устраивает примерный Vpp — пропусти, показания и так соответствуют реальности. Если хочешь, чтобы Vpp на экране совпадал с мультиметром до сотых — откалибруй один раз.

**Как откалибровать (один раз, ~30 с):**

1. Залей прошивку **v0.6.0 или новее** (это делает приложение — см. «Быстрый старт») и нажми **CONNECT**.
2. Выстави любой уровень Vpp — например, выбери **p1** в списке и нажми **ОТПРАВИТЬ НАСТРОЙКИ**. (Уровень не важен: калибровочный коэффициент одинаков на всех уровнях. Только не вставляй чип в панель, пока поднимаешь напряжение.)
3. Приложи мультиметр к контрольной точке Vpp и считай **реальное** напряжение **сейчас**.
4. Нажми **`[ CAL V ]`**. В диалоге показано, какое Vpp плата думает сейчас, и просьба ввести реальное — впиши число, которое мультиметр показывает **в этот момент** (напр. `12.50`), и подтверди.
5. Готово. Приложение сохранит коэффициент, перечитает Vpp и проверит, что теперь сходится. Значение лежит в EEPROM навсегда (пока не перекалибруешь).

**Заметки.**
- Вводить нужно напряжение, **измеренное прямо сейчас**, а не ожидаемое/запомненное — прошивка сравнивает твоё число с тем, что читает в этот миг. (В диалоге показано живое значение — чтобы не ошибиться.)
- Калибровка не зависит от уровня: откалибровав на p1, получаешь верными все уровни (p0…p7).
- Команда по Serial (Arduino IDE / любой терминал): `T<сантивольты>` — напр. `T1250` для 12.50 В; `T0` сброс к дефолту; одиночное `T` печатает текущий коэффициент.
- Если приложение предупредит, что после калибровки показание сильно мимо (напр. ~в 100 раз больше введённого) — значит прошивка на плате не совпала с приложением: перешей Nano кнопкой **FLASH NANO** и повтори.

---

## Сверка на Retro Chip Tester

КР556РТ4 — советский аналог **82S129 / N82S129** (биполярный ППЗУ 256×4), так что на [Retro Chip Tester](https://www.8bit-museum.de/) выбирай эту модель для чтения/опознания.

RCT считает **CRC-32**, и оно совпадает с **`file (4-bit)`** CRC-32, который приложение показывает в HEX-панели (проверено на железе). Поэтому для сверки дампа с RCT сравнивай контрольную сумму тестера именно с **`file (4-bit)`**, а не с `raw file` (РТ4 хранит лишь младшие 4 бита ячейки, поэтому «сырая» 8-битная CRC с тестером не сойдётся).

---

## О безопасности

КР556РТ4 однократное. Прочти один раз:

- Прожиг **необратим**. Сначала перепроверь файл и чип.
- Высокое напряжение доходит до чипа только во время самих импульсов программирования (через линию WRITE). Выставление уровня или чтение чипа не подаёт его на панельку.
- Для единственного хорошего чипа сначала прогони на жертвенном чистом, считай обратно и сверь CRC с исходником, прежде чем жечь основной.

---

## Планы

- **Защита от перегрева (DS18B20).** В оригинальном Diduino были пины под датчик температуры DS18B20. С установленным датчиком прошивка могла бы следить за температурой чипа или силового транзистора во время прожига и останавливаться при перегреве. Особенно это к месту с soak-импульсами, которые греют перемычку и ключ. На текущей плате датчик не распаян, поэтому оставлено на будущую ревизию.

---

## Лицензия

BSD 2-Clause. Оригинальный копирайт — Науменко Андрей (2021); прошивка и приложение Nichrome под той же лицензией. См. [LICENSE](LICENSE).

## Автор

**Александр Лавринович** · <lavrinovich.alex@gmail.com> · [github.com/Alex-Electron](https://github.com/Alex-Electron)
Соавтор: AI.

## Контрибьюторы

- **[pahan4](https://github.com/pahan4)** (Павел) — идеи веб-прошивки Arduino прямо из браузера (обновление прошивки из приложения, через Web Serial), встраивания прошивок в один HTML-файл (офлайн, самодостаточный), split-вида сверки (файл ↔ чип) и модальных окон в стиле приложения (вместо браузерных confirm/prompt).

<sub>Русская версия README переведена с английского с помощью ИИ.</sub>
