# Arduino build output (the firmware is embedded into docs/diduino_nichrome.html by build-hex.ps1,
# so the standalone .hex artifacts are local build outputs only — not committed)
build/
*.hex
*.elf

# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~
